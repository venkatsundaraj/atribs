import AllVehicles from "@/app/(dashboard)/_components/all-vehicles";
import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return <AllVehicles />;
};

export default page;
